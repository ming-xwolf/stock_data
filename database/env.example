# 数据库配置
# 复制此文件为 .env 并修改相应的配置

# ============================================
# Supabase (PostgreSQL) 配置 - 默认数据库
# ============================================
# 方式1: 使用连接 URI（推荐）
# SUPABASE_URI=postgresql://user:password@host:port/database

# 方式2: 使用单独的连接参数
# SUPABASE_HOST=your-supabase-host.supabase.co
# SUPABASE_PORT=5432
# SUPABASE_USER=postgres
# SUPABASE_PASSWORD=your-password
# SUPABASE_DATABASE=postgres

# 或者使用通用环境变量名（也支持）
# DATABASE_URL=postgresql://user:password@host:port/database
# DB_HOST=your-supabase-host.supabase.co
# DB_PORT=5432
# DB_USER=postgres
# DB_PASSWORD=your-password
# DB_NAME=postgres

# ============================================
# Dolt (MySQL) 配置 - 备用数据库
# ============================================
# 如需使用 Dolt 数据库，设置 DB_TYPE=dolt
# DB_TYPE=dolt
# DOLT_HOST=192.168.2.37
# DOLT_PORT=13306
# DOLT_USER=root
# DOLT_ROOT_PASSWORD=test
# DOLT_DATABASE=a_stock
# DOLT_CHARSET=utf8mb4
